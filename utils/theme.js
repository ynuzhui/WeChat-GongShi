// 主题色常量：供 JS 逻辑（如 wx.showModal 的 confirmColor）引用；
// CSS 侧统一使用 app.wxss 中的 --app-* token，请勿在样式里硬编码。
const PRIMARY_COLOR = '#5B5BD6'
const DANGER_COLOR = '#E5484D'

module.exports = {
  PRIMARY_COLOR,
  DANGER_COLOR
}
