# Profile.js 重构建议

## 当前状态

- **文件大小**：732 行
- **主要功能**：预设管理、月度余额、数据导出、Canvas 报表、备份恢复

## 重构方案

### 推荐方案：按功能拆分模块

```
pages/profile/
├── profile.js           # 主页面（约 200 行）
├── preset-manager.js   # 预设管理
├── export-manager.js   # 文本导出
├── report-canvas.js    # 图片报表
└── backup-manager.js   # 备份恢复（已部分完成）
```

每个模块职责单一，易于测试和维护。

## 实施建议

1. 先提取 Canvas 绘图逻辑（约减少 150 行）
2. 再提取备份和导出逻辑
3. 为每个模块编写单元测试
4. 主页面只保留生命周期和调度逻辑

详细方案见项目文档。